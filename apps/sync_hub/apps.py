from django.apps import AppConfig


class SyncHubConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.sync_hub'
    verbose_name = 'مرکز همگام‌سازی و انتقال تغییرات'
